<div class="login">

  <h2><div class="logo02"></div>
    <div class="logo_text">TIMENOTE</div></h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>

<div class="field">
    <%= f.text_field :name, autofocus: true, placeholder: "ニックネーム",size: 30 %>
  </div>

  <div class="field">
    <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "メールアドレス",size: 30 %>
  </div>

  <div class="field">
    <%= f.password_field :password, autocomplete: "current-password", placeholder: "パスワード(6文字以上)",size: 30  %>
  </div>

  <div class="field">
    <%= f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "パスワード確認用",size: 30 %>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :パスワードを記憶する %>
    </div>
  <% end -%>

  <div class="actions">
    <%= f.submit "新規登録", class: "btn btn-dark btn-block" %>
  </div>
<% end %>

<%= render "users/shared/links" %>

</div>
